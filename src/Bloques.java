//@albertoss1

/*import java.util.Scanner;
import java.util.HashMap;
import java.util.Set;

public class Bloques {
    public static void main(String [] arg){
        Scanner scan = new Scanner(System.in);
        HashMap<Integer, Integer> h = new HashMap<>();
        int n,contador = 0;
        n = scan.nextInt();
        for(int i = 0;i < n; i++){  //vista frontal
            //Guardar valor 1 en hashmap(si es nuevo)
            if (h.containsKey(n)) {  //+1 si el hashmap(si no es nuevo)
                h.get(n);
                h.replace(n,n+1);
            }
        }
        for(int j = 0;j < n; j++){  //vista lateral
            if(!h.containsKey(n)){ //hashmap 1 si no encontrado
                h.put(n,1);
            }
            if(hashmap[i] < 0){
                suma += v[i];
            }
                            //if(hashmap[i] < 0)
                                //suma += v[i]
        }
    }
}*/